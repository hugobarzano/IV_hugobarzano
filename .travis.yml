language: python
python:
 - "2.7"

install:
 - pip install -r requirements.txt

script:
 - python manage.py test 

deploy:
  provider: openshift
  user: "hugobarzano@gmail.com"
  password: "root1993" 
  domain: "hugobarzano"
  app: computer

after_deploy:
  - rhc env set OPENSHIFT_PYTHON_SERVER=gunicorn -a computer
  - rhc app restart -a computer
  - python manage.py syncdb --noinput
    
